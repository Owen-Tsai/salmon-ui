import { ref } from 'vue'

const initDropdownDomEvents = (
  dropdownMenu, dropdownTriggerEl, dropdownInstance
) => {
  const menuItems = ref<any>(null)
  const menuItemsArray = ref<any>(null)
  const dropdownEl = ref<any>(null)

}
