// colors
color--primary = #2258e3
color--success = #06ba63
color--error = #E63333
color--warning = #FFBE4D

colors--primary = #e6f0ff,
  #a1c5ff,
  #77a5fc,
  #4a7ff0,
  #2258e3,
  #133dbd,
  #082696,
  #001570,
  #000b4a

colors--success = #d5eddf,
  #72e0a0,
  #4ad488,
  #26c774,
  #06ba63,
  #009451,
  #006e40,
  #00472c,
  #002116

colors--error = #ffe9e6,
  #ffbab3,
  #ff928a,
  #f2615c,
  #e63333,
  #bf2126,
  #99121b,
  #730813,
  #4d050e

colors--warning = #fffbf0,
  #fff0c7,
  #ffe29e,
  #ffd175,
  #ffbe4d,
  #d99836,
  #b37524,
  #8c5515,
  #663a0e

colors--neutral = #ffffff,
  #F9FAFB,
  #F3F4F6,
  #E5E7EB,
  #D1D5DB,
  #9CA3AF,
  #6B7280,
  #4B5563,
  #374151,
  #1F2937,
  #111827,
  #000000

color--primary-lighter = colors--primary[0]
color--success-lighter = colors--success[0]
color--warning-lighter = colors--warning[0]
color--error-lighter = colors--error[0]

color--primary-light = colors--primary[4]
color--success-light = colors--success[4]
color--warning-light = colors--warning[4]
color--error-light = colors--error[4]

color--primary-dark = colors--primary[6]
color--success-dark = colors--success[6]
color--warning-dark = colors--warning[6]
color--error-dark = colors--error[6]

color--primary-darker = colors--primary[8]
color--success-darker = colors--success[8]
color--warning-darker = colors--warning[8]
color--error-darker = colors--error[8]

color--text = colors--neutral[9]
color--text-light = colors--neutral[6]

color--disabled = colors--neutral[5]
color--border = colors--neutral[4]
color--divider = colors--neutral[3]

color--surface = colors--neutral[2]
color--surface-light = colors--neutral[1]
color--surface-dark = colors--neutral[3]

typesMap = {
  'default': colors--primary,
  'success': colors--success,
  'warning': colors--warning,
  'error': colors--error
}

// typography
font-size--display = {
  larger: 48px,
  large: 36px,
  medium: 24px,
  small: 22px
}

font-size--text = {
  larger: 18px,
  large: 16px,
  medium: 14px,
  small: 12px,
}

icon-size = {
  small: 16px,
  medium: 18px,
  large: 20px,
  larger: 24px
}

font-family = -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial,
        'Noto Sans', sans-serif, 'Apple Color Emoji', 'Segoe UI Emoji', 'Segoe UI Symbol',
        'Noto Color Emoji'

border-radius--normal = 4px
border-radius--large = 8px

box-shadow--normal = 0 4px 8px rgba(0, 0, 0, 16%)

box-shadow--elevation = 0 3px 6px -4px rgba(0, 0, 0, 12%), 0 6px 16px 0 rgba(0, 0, 0, 8%), 0 9px 28px 8px rgba(0, 0, 0, 5%)
box-shadow--primary = 0 0 0 0.25rem rgba(color--primary, 0.2)
